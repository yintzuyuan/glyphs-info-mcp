# Glyphs info MCP Server Environment Variables

# ═══════════════════════════════════════════════════════════════════
# 資料來源配置 - Native Accessor 系統
# ═══════════════════════════════════════════════════════════════════
# 本 MCP 伺服器採用 Native Accessor 架構，直接讀取原始資料來源，
# 無需 JSON 中間層。詳細資料來源說明請參考 DATA_SOURCES.md

# 資料路徑配置（選用）
# GLYPHS_MCP_DATA_PATH=/custom/path/to/data

# Glyphs Repositories 路徑（自動偵測 Glyphs 3，通常無需設定）
# 用於 Vanilla UI 元件讀取
# 只有在使用非標準位置時才需要設定
# GLYPHS_REPOSITORIES_PATH="${HOME}/Library/Application Support/Glyphs 3/Repositories"

# Glyphs 3 Objective-C Headers 路徑（自動偵測，通常無需設定）
# 預設: /Applications/Glyphs 3.app/Contents/Frameworks/GlyphsCore.framework/Versions/A/Headers
# GLYPHS_APP_HEADERS_PATH="/Applications/Glyphs 3.app/Contents/Frameworks/GlyphsCore.framework/Versions/A/Headers"

# mekkablue 腳本集路徑（自動偵測，通常無需設定）
# 用於程式碼範本參考
# 只有在使用非標準位置時才需要設定
# MEKKABLUE_SCRIPTS_PATH="${HOME}/Library/Application Support/Glyphs 3/Repositories/mekkablue"

# GlyphsSDK 路徑（內嵌於專案中，無需配置）
# Python API, Plugin API, Drawing Tools, Plugin Templates 等資料
# 位於: modules/glyphs-api/src/resources/GlyphsSDK

# 效能調校
# GLYPHS_MCP_MAX_SEARCH_RESULTS=100
# GLYPHS_MCP_SEARCH_TIMEOUT=60
# GLYPHS_MCP_ENABLE_CACHE=true

# 日誌設定
# GLYPHS_MCP_LOG_LEVEL=INFO

# MCP 伺服器設定
# MCP_SERVER_NAME=glyphs-info-mcp
# MCP_SERVER_VERSION=1.0.0

# 開發環境設定
# DEVELOPMENT_MODE=false
# DEBUG_MODE=false